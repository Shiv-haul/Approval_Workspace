<%- include('partials/header') %>

<div class="text-center py-5">
  <div class="display-1 text-secondary mb-3">📝</div>
  <h2>Note for Approval (NFA)</h2>
  <p class="lead text-muted">Parked for Phase 2.</p>
  <p>
    Drop your sample NFA Excel template in <code>02-templates/</code> and we'll mirror the PR build kit for it.
    The approval engine (Project Team → Verifier → Approver) carries over unchanged; only the form schema differs.
  </p>
  <a href="/" class="btn btn-primary mt-3">← Back to home</a>
</div>

<%- include('partials/footer') %>
